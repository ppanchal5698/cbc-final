"""Vectorless page index over the vendor catalogs.

The catalogs are the source of truth. This says what is on each page, so a
pricing pass opens the two pages that matter instead of scanning 744, and reads
the price off the sheet rather than out of a pre-extracted table that may have
misread it.
"""
