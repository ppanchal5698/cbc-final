"""Pydantic contracts (formerly `cbc.schemas`).

Import shims keep `cbc.schemas` working for one release. JSON Schema under
`schemas/artifacts/` is generated from these models.
"""
from cbc.schemas import *  # noqa: F401,F403
