"""The phase modules behind `cbc.services.sync`.

Import from `cbc.services.sync`, not from here - it is the module both
applications already name, and it aggregates these four so a caller does not
have to know which phase a function belongs to.
"""
