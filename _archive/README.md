# _archive

Original copies of anything the agent-config audit removed from its working
location. Paths mirror the original layout, so `_archive/.claude/rules/foo.md`
was once `.claude/rules/foo.md`.

Each archived file carries a timestamp header saying when and why it moved.
Nothing here is loaded by any agent. See `../CLEANUP_CHANGELOG.md` for the log.
